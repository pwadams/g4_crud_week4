<div><% @events.each do |event| %>
  <div>
    <%= event.Description %>
    <div class="col-md-1"><%=link_to "Show", event_path(event) %>
          <%=link_to "Edit", edit_event_path(event) %> </div>
          <%=link_to "Destroy", event, method: :delete %> </div>

  </div>
<hr>
<% end %>
<p><%= link_to "new event", new_event_path %></p>
